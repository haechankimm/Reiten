# 별도 프로젝트 (자체 git 저장소를 이미 가지고 있음 — 여기서는 추적하지 않음)
expend app/

# OS
.DS_Store

# iCloud Drive 동기화 충돌 시 자동 생성되는 중복 사본, 백업용 zip
*.zip
소스 코드 2/
소스 코드 2

# Node
node_modules/

# 비밀값 (Supabase/Resend/Cloudinary 키 등)
.env
.env.local
